$(document).ready(function () {
    $(".submit").on("click", function (event) {
        event.preventDefault()

        let name = $("#name").val().trim()
        let scores = []

        let score = $(this).

        scores.push(score)

        socres.push($(".surveyScores").val())


        console.log(name)
        console.log(scores)
    })
})